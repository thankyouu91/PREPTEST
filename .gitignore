node_modules/
npm-debug.log*
.DS_Store
*.local
# Chỉ bỏ qua CSDL chạy ở gốc repo. KHÔNG dùng "data/" trần vì nó khớp cả
# server/data/ (dữ liệu nguồn phải được commit).
/data/

# Cấu hình riêng từng máy cho script Windows (có thể chứa mật khẩu quản trị).
# Bản mẫu cau-hinh.mau.bat thì vẫn commit.
cai-dat/cau-hinh.bat

# Ảnh nghiệm thu do `npm run screenshot` sinh ra. KHÔNG commit: 86 ảnh chụp lại
# mỗi lượt chạy cổng kiểm thử, và lịch sử của chúng từng chiếm 575 MB — tức là
# 95% khối lượng repo, cho những tệp tái tạo được bất cứ lúc nào bằng một lệnh.
docs/screenshots/

# Piper voice models: ~180 MB, fetched on demand — see scripts/make-vpet-audio.mjs
.piper-voices/
